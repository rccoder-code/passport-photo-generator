# passport-photo-generator
A Streamlit-based application that takes a single passport photo, automatically crops and resizes it to standard dimensions, and generates a print-ready A4 sheet containing multiple copies for easy printing.
